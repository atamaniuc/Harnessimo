# The commit from which the locked-surface rule applies.
#
# Moved forward four times, each deliberately and each recorded: to adopt the
# shared CLI in CI; to switch GitHub Pages on from the workflow, build the docs
# strictly and add the release job; to add the typecheck job; and now for the
# TypeScript migration, which changes how every job runs the tool and adds a
# build the release must verify. Widening what an agent may change is a
# decision, and this line is where the decision is recorded. The fifth move
# decision, and this line is where the decision is recorded. The fifth move
# adds publish.yml and changes how the release ships the package; the sixth
# decision, and this line is where the decision is recorded. The fifth move
# adds publish.yml and changes how the release ships the package; the sixth
# prepares publishing to authenticate by OIDC; the seventh removes the stored
# decision, and this line is where the decision is recorded. The fifth move
# adds publish.yml and changes how the release ships the package; the sixth
# prepares publishing to authenticate by OIDC; the seventh removes the stored
# token; the eighth makes publishing release-only and idempotent, after three
# decision, and this line is where the decision is recorded. The fifth move
# adds publish.yml and changes how the release ships the package; the sixth
# prepares publishing to authenticate by OIDC; the seventh removes the stored
# token; the eighth makes publishing release-only and idempotent; the ninth
# adds ops.yml, which does what a session may not — tags, refs, repository
# settings — under a token that never enters a session's context.
# The tenth records that publishing checks for a release rather than trusting
# a trigger that, created by a workflow, never fires.
# The eleventh points the operations workflow at the secret's real name.
# The twelfth makes the workflows fetch the tags the release check reads.
98a575ac8790ac4b7999819b74396dfc6e7552a2
